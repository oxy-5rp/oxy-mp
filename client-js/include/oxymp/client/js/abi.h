/* Граница между клиентом и его скриптовой машиной.
 *
 * На C, а не на C++, и это не дань привычке. Клиент собирается со статической
 * библиотекой времени выполнения (`/MT`) — иначе Windows не найдёт ему
 * `msvcp140.dll`, потому что ищет её рядом с `GTA5.exe`, а не рядом с ним. А
 * машина обязана быть динамической (`/MD`): рядом с ней живёт `libnode.dll`,
 * собранный так же, и иначе Node не собирается вовсе. Подробности — в
 * `cmake/dual_runtime.cmake`.
 *
 * Смешать `/MT` и `/MD` в одном бинарнике нельзя, поэтому машина живёт отдельной
 * DLL. А раз она отдельная и куча у неё своя, через границу не имеет права
 * ходить ничего, что выделено одной стороной и освобождается другой:
 * `std::string`, `std::vector`, умный указатель. Выделено там, освобождается
 * здесь — это не «немного неаккуратно», а порча памяти, которая проявится за
 * час игры и в другом месте.
 *
 * Отсюда правила, которые здесь соблюдаются без исключений:
 *
 *   - только типы C и указатели на них;
 *   - строки и буферы передаются указателем и длиной, и **принадлежат
 *     передающему**: получатель обязан либо употребить их сразу, либо
 *     скопировать себе;
 *   - строки не обязаны оканчиваться нулём — длина главнее;
 *   - ни одна сторона не освобождает памяти другой.
 *
 * Так же устроен и alt:V: его клиентский `js-module.dll` — отдельная библиотека
 * ровно по этой же причине.
 */

#ifndef OXYMP_CLIENT_JS_ABI_H
#define OXYMP_CLIENT_JS_ABI_H

#include <stdint.h>

#ifdef __cplusplus
extern "C" {
#endif

/* Версия границы.
 *
 * Проверяется при загрузке, и несовпадение — отказ, а не попытка. Клиент и
 * машина раздаются вместе, но у человека на диске может оказаться DLL от
 * прошлой сборки, и разобрать чужую раскладку структуры она попытается молча.
 *
 * Увеличивается при всяком изменении состава или порядка полей ниже.
 */
#define OXYMP_CLIENT_JS_ABI_VERSION 7u

/* Строка: указатель и длина. Нулём оканчиваться не обязана. */
typedef struct OxympJsText {
    const char* data;
    uint32_t length;
} OxympJsText;

/* Двоичные данные: указатель и длина. */
typedef struct OxympJsBytes {
    const uint8_t* data;
    uint32_t length;
} OxympJsBytes;

/* Уровень строки в журнале. Числа те же, что у уровней spdlog по смыслу. */
typedef enum OxympJsLogLevel {
    kOxympJsLogInfo = 0,
    kOxympJsLogWarning = 1,
    kOxympJsLogError = 2,
    kOxympJsLogDebug = 3
} OxympJsLogLevel;

/* Сколько ячеек вмещает вызов натива.
 *
 * То же число, что у NativeContext на стороне клиента: доводы кладутся в буфер
 * одинаковых восьмибайтовых ячеек, и обе стороны обязаны считать его размер
 * одинаково.
 */
#define OXYMP_JS_NATIVE_CELLS 32u

/* Род сущности сессии.
 *
 * Тот же перечень, что и у метаданных, только числом вместо слова: сервер
 * рассылает `syncedMeta` под именами `player` и `vehicle`, и слой на JavaScript
 * переводит одно в другое. Числа здесь закрепляются навсегда — по той же
 * причине, по какой закрепляются номера сообщений протокола.
 */
typedef enum OxympJsEntityKind {
    kOxympJsEntityPlayer = 0,
    kOxympJsEntityVehicle = 1,

    /* Прохожие, заведённые сервером. Не те, что расставила игра: у этих есть
     * номер сессии, они одни и те же у всех игроков, и распоряжается ими
     * сервер.
     */
    kOxympJsEntityPed = 2
} OxympJsEntityKind;

/* Сущность сессии: чем её зовёт сервер и чем её зовёт игра.
 *
 * Двумя числами, и оба нужны. Номер выдаёт сервер, он один для всех и им
 * сущность называют в событиях и метаданных. Дескриптор выдаёт игра, он у
 * каждого игрока свой и только им можно позвать натив. Переводчик между ними —
 * это и есть то, чего клиенту недоставало, чтобы знать вокруг себя кого-то,
 * кроме себя.
 */
/* Состояние игрока сессии, каким его знает клиент.
 *
 * Одной структурой, а не вызовом на поле, и это не бережливость: спрашивают их
 * из кадра — режимы рисуют значки над головами и проверяют прицел каждый кадр, —
 * и два десятка переходов через границу на игрока стоили бы дороже всей
 * отрисовки.
 *
 * Берётся из снимка, а не у игры, и это единственно верно: чужой персонаж здесь
 * кукла, которой распоряжаемся мы сами, и спросить у игры «целится ли он» значит
 * спросить, что мы сами ей велели. Правду знает только хозяин, и она приезжает
 * снимком.
 */
typedef struct OxympJsPlayerState {
    /* Ноль — игрока с таким номером клиент не знает. Всё остальное тогда
     * бессмысленно, и читать его нельзя.
     */
    int32_t known;

    /* Набор shared::PlayerFlag. */
    uint32_t flags;

    /* Куда он целится, а не целясь — куда смотрит. Точка в мире, а не
     * направление: одно поле на две надобности, и различает их признак прицела.
     * Поворота головы отдельно в снимке нет — его и не спрашивают.
     */
    float aimX;
    float aimY;
    float aimZ;

    /* Куда повёрнуто тело. Градусы, как у игры. */
    float heading;

    /* Скорость по осям мира, метры в секунду. */
    float velocityX;
    float velocityY;
    float velocityZ;

    uint32_t weapon;
    uint16_t health;
    uint16_t armour;
} OxympJsPlayerState;

typedef struct OxympJsEntity {
    int32_t id;

    /* Номер игрока, который сущность ведёт. −1 — никто.
     *
     * Едет тем же списком, что и дескриптор, а не отдельным вопросом: смену
     * ведущего ищут перебором по всем сущностям каждый кадр, и переход через
     * границу на каждую из них стоил бы дороже самого перебора.
     */
    int32_t owner;

    /* Ноль — сущность в сессии есть, а тела у неё здесь ещё нет: модель могла
     * не догрузиться, а игрок — стоять за пределами подгрузки. Это не ошибка, и
     * у alt:V такая сущность тоже есть, просто её `scriptID` равен нулю.
     */
    int32_t handle;
} OxympJsEntity;

/* Что машина просит у клиента.
 *
 * Заполняет клиент, зовёт машина. Указатель `context` возвращается в каждом
 * вызове неизменным — через него клиент находит себя, не заводя глобальных.
 */
typedef struct OxympJsHost {
    void* context;

    /* Строка в журнал клиента. */
    void (*log)(void* context, OxympJsLogLevel level, OxympJsText resource, OxympJsText line);

    /* Именованное событие серверу. Нагрузка — уложенный набор доводов. */
    void (*emitServer)(void* context, OxympJsText name, OxympJsBytes payload);

    /* Вызов натива игры.
     *
     * Доводы и результаты — ячейками по восемь байт, как их и принимает сама
     * игра. Толкование ячеек — забота зовущего: игра не знает типов, она знает
     * ширину.
     *
     * Возвращает 0 при отказе — натив не разрешился в адрес. Ноль отличается от
     * «натив вернул ноль» именно тем, что при отказе `results` не трогается.
     */
    int32_t (*callNative)(void* context, uint64_t hash, const uint64_t* arguments,
                          uint32_t argumentCount, uint64_t* results, uint32_t resultCapacity);

    /* Наш номер в сессии — тот, которым нас зовёт сервер.
     *
     * Спрашивается, а не присылается, и это существенно. Ресурс читает его
     * первыми же строками, ещё до того, как до него дойдёт хоть одно событие;
     * присланный событием, он застал бы его пустым. К тому же номер меняется —
     * при переподключении сервер выдаёт другой, — и присланный однажды устарел
     * бы молча.
     *
     * Возвращает -1, пока сервер нас не принял.
     */
    int32_t (*localPlayerId)(void* context);

    /* Читает файл ресурса из кеша клиента.
     *
     * Отдаёт указатель на содержимое, которое принадлежит клиенту и живо до
     * следующего вызова этого же метода. Пусто — файла нет.
     */
    OxympJsBytes (*readResourceFile)(void* context, OxympJsText resource, OxympJsText file);

    /* Сущности сессии этого рода — все, о которых клиенту сказал сервер.
     *
     * Заполняет не больше `capacity` записей, а возвращает столько, сколько их
     * есть всего. Возврат больше `capacity` — это «буфер мал», и зовущий обязан
     * позвать заново с бо́льшим. Молчаливого обрезания здесь нет намеренно:
     * список ресурсов однажды обрезался на 512 файлах именно так, и искали это
     * долго — обрезанный список выглядит как полный.
     */
    uint32_t (*listEntities)(void* context, OxympJsEntityKind kind, OxympJsEntity* entities,
                             uint32_t capacity);

    /* Дескриптор игры по номеру сессии. 0 — сущности нет или тела у неё ещё нет.
     *
     * Спрашивается на каждое обращение, а не запоминается машиной. Так надо:
     * тело игра вправе убрать и завести заново — при подгрузке мира, при смерти,
     * просто по своему усмотрению, — и дескриптор при этом меняется. Сохранённый
     * однажды, он указывал бы в никуда до конца сессии.
     */
    int32_t (*entityHandle)(void* context, OxympJsEntityKind kind, int32_t id);

    /* Номер сессии по дескриптору игры. -1 — эта сущность не из сессии.
     *
     * Обратный перевод нужен не меньше прямого: нативы отдают дескрипторы —
     * луч, попавший в персонажа, вернёт число, — и без него ресурс не узнал бы,
     * в кого попал.
     */
    int32_t (*entityId)(void* context, OxympJsEntityKind kind, int32_t handle);

    /* Имя игрока сессии. Пусто — такого игрока нет.
     *
     * Строка принадлежит клиенту и жива до следующего вызова этого же метода —
     * как и у readResourceFile.
     */
    OxympJsText (*playerName)(void* context, int32_t id);

    /* Состояние игрока сессии. `known` равен нулю — такого игрока клиент не
     * знает.
     */
    OxympJsPlayerState (*playerState)(void* context, int32_t id);

    /* Заводит окно интерфейса поверх кадра. Возвращает его номер, 0 — отказ. */
    uint32_t (*createWebView)(void* context, OxympJsText resource, OxympJsText url);

    /* Убирает окно. */
    void (*destroyWebView)(void* context, uint32_t view);

    /* Событие странице окна. */
    void (*emitWebView)(void* context, uint32_t view, OxympJsText name, OxympJsBytes payload);

    /* Показывать ли окно и брать ли им мышь. */
    void (*setWebViewVisible)(void* context, uint32_t view, int32_t visible);
    void (*setWebViewFocused)(void* context, uint32_t view, int32_t focused);

    /* Просит показать указатель мыши или отпускает эту просьбу.
     *
     * Счётчиком, а не признаком: два окна режима просят его независимо, и
     * закрытие одного не должно отнимать указатель у второго. Так же считает и
     * alt:V.
     *
     * Нужно отдельно от setWebViewFocused, потому что просьбы разные: внимание
     * отдают одному окну, а указатель показывают поверх всего кадра — и
     * попросить его может режим, у которого окна нет вовсе.
     */
    void (*showCursor)(void* context, int32_t show);
} OxympJsHost;

/* Что умеет машина.
 *
 * Заполняет машина, зовёт клиент. Все вызовы — из одного потока, того самого, на
 * котором идёт такт клиента: движок V8 не переносит обращения из двух потоков
 * без блокировки, а заводить её ради того, чего не бывает, незачем.
 */
typedef struct OxympJsEngine {
    /* Поднимает машину. 0 — отказ. Зовётся один раз за жизнь процесса. */
    int32_t (*setUp)(void);

    /* Поднимает ресурс: имя, корень в кеше и точка входа от корня.
     *
     * Причина отказа уходит в журнал клиента через host->log, а не возвращается
     * строкой: строка через границу потребовала бы владения, а владеть ей здесь
     * некому.
     */
    int32_t (*startResource)(OxympJsText name, OxympJsText root, OxympJsText entry);

    /* Останавливает ресурс. */
    void (*stopResource)(OxympJsText name);

    /* Даёт движку поработать: таймеры, обещания, ввод-вывод. Раз в такт. */
    void (*tick)(void);

    /* Событие от сервера — всем поднятым ресурсам. */
    void (*dispatchServerEvent)(OxympJsText name, OxympJsBytes payload);

    /* Событие от страницы интерфейса. */
    void (*dispatchWebViewEvent)(uint32_t view, OxympJsText name, OxympJsBytes payload);

    /* Событие сессии без нагрузки: connectionComplete, disconnect и прочие. */
    void (*dispatchSessionEvent)(OxympJsText name);

    /* Нажата или отпущена клавиша. */
    void (*dispatchKey)(uint32_t key, int32_t down);

    /* Разбирает всё и отпускает движок. */
    void (*tearDown)(void);
} OxympJsEngine;

/* Единственное, что машина выставляет наружу.
 *
 * Одна точка входа, а не десяток экспортов: так проверка версии случается
 * раньше первого обращения к чему бы то ни было, и раскладку структур ниже
 * никто не успевает истолковать по-своему.
 *
 * Возвращает NULL, если версия границы не совпала.
 */
typedef const OxympJsEngine* (*OxympJsEntry)(uint32_t abiVersion, const OxympJsHost* host);

/* Имя этой функции в таблице экспорта. */
#define OXYMP_CLIENT_JS_ENTRY_NAME "oxympClientJsEntry"

#ifdef __cplusplus
} /* extern "C" */
#endif

#endif /* OXYMP_CLIENT_JS_ABI_H */
