#pragma once

namespace oxymp::client::js {

/// Поднимает libnode.dll, если её ещё нет в процессе. false — не нашлась.
///
/// Зовётся до первого обращения к чему бы то ни было из Node. Библиотека
/// подключена с подгрузкой по требованию (`/DELAYLOAD`), и без этого вызова
/// первое же обращение к ней дало бы не отказ, а исключение Windows «модуль не
/// найден» — то есть вылет игры вместо строки в журнале.
[[nodiscard]] bool ensureNodeLibrary();

} // namespace oxymp::client::js
