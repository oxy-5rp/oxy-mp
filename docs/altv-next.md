# Совместимость с alt:V: что дальше

Записка для того, кто продолжит. Что сделано — в `docs/scripting.md`, раздел
«Совместимость с alt:V»; здесь только незакрытое, по убыванию важности.

## 1. Измерения (dimensions) — сделано

Есть на сервере и в скриптовом слое: `player.dimension`, `vehicle.dimension`,
числа те же, что у alt:V (`alt.defaultDimension` — 0, `alt.globalDimension` —
−2147483648). Правило видимости — `script/include/oxymp/script/dimension.hpp`:
видят друг друга те, кто в одном слое, либо тот, кто в общем.

По измерениям разложено всё, чем сервер рассказывает о мире: снимки игроков,
раздача машин и предметов, рассылка рядом. Ушедший в другой слой пропадает у
остальных сам собой — им просто перестают приходить его снимки, и клиент
убирает замолчавшего тем же порядком, что и отошедшего за горизонт.

**Клиент про измерения не знает вовсе, и это решение, а не пробел.** Не
рассказать ему о том, чего он не должен видеть, — единственный способ не дать
ему это увидеть; расскажи мы, а прятать поручи ему, — и первый же изменённый
клиент увидел бы всех. Поэтому `entity.dimension` на клиенте отказывает на
запись вслух: переставляет сервер.

**Зоны слой мира спрашивают** — закрыто 21 августа 2026. Прежде зона одной
квартиры ловила гостя другой, стоящей в той же точке.

**Смена слоя переспрашивает нарисованное** — тоже закрыто. Всё, что отбирается
расстоянием, разбирается само собой: снимки перестают приходить, машины уезжают
за горизонт видимости. Метки, маркеры и точки не отбираются — они уходят один
раз, при входе, — и без отдельного повода игрок унёс бы карту прежнего слоя с
собой навсегда.

## 2. Имена нативов, которых нет в открытой базе

`tools/nativegen/nativetable.py`, словарь `ALT_ALIASES`. Пять штук уже там;
встретится шестой — добавьте туда же, а не подставляйте хеш руками.

Как узнать имя у базы: найти хеш в `@altv/types-natives` не выйдет — там их нет.
Ищется по смыслу в `gta_universal.lua` (см. `_SCREEN_DRAW_POSITION_BEGIN` для
`setScriptGfxAlign`). **Угадывать нельзя**: неверный хеш даёт не ошибку, а вылет
игры в мгновение вызова.

Незакрытое, замеченное на VRUSSIA: `getScriptGfxAlignPosition`.

## 3. Что выяснилось на живом режиме VRUSSIA

Проверялось так: `oxymp-server` поднимался прямо из каталога живого режима — он
читает `server.toml` у alt:V сам, если своего `server.cfg` рядом нет.

**Клиентская половина проходит целиком и без единой ошибки.** Ресурс поднимается,
1879 файлов раздаются, страница интерфейса открывается, её роутер работает,
камера входа ставится. До этого мешали три вещи, и все три закрыты:
`getScriptGfxAlignPosition` (исключение каждый кадр), `player.model` (падал
обработчик входа целиком) и двери зданий.

**Отказ посреди чужого обработчика стоит дороже, чем кажется.** `player.model`
бросал — и вместе с ним не исполнялось всё, что шло следом: показ интерфейса,
рассылка дверей, объявление игрока вошедшим. Снаружи это выглядело как «ничего
не работает», хотя не работала одна строка. Отсюда правило: то, что исполняется
наполовину, **говорит об этом один раз в журнал и не бросает** (`warnOnce` в
`alt_server.js`); бросает только то, чего нет вовсе.

**Страница рисуется — это проверено отдельно.** В журнал выводится размер
первого кадра окна и число видимых точек в нём: страница, залитая прозрачным,
приходит такими же кадрами, как и страница с картинкой, и снаружи их не
различить. Показательный режим `altv-demo` рисуется в кадре полностью.

**База данных живого режима — не наша забота, но без неё ничего не увидеть.** У
VRUSSIA это PostgreSQL, и без неё её серверная половина не поднимается:
`DB connect error`, десять попыток из десяти. Пока она не поднялась, интерфейс
режима остаётся пустым — и выглядит это ровно так же, как если бы его не рисовали
мы. Отличить одно от другого и позволяет строка про видимые точки.

## 4. Чего нет вовсе

| Что | Замечание |
|---|---|
| Голос | `player.isTalking` отвечает «нет» — это правда, связи нет |
| `alt.Audio` | Звук ресурса |
| ~~`alt.HttpClient`, `WebSocketClient`~~ | **Сделано.** Поверх `http`/`https` и `WebSocket` из Node |
| `RmlUi` | Второй способ рисовать интерфейс у alt:V |
| ~~`alt.Ped`~~ | **Сделано.** Куклы стоят там, где их поставили: ведущего у них нет |
| ~~`attachTo`~~ | **Сделано.** Своей кости нет: натива «кость к кости» в сборке не нашлось |
| ~~Обвесы машины (`setMod`)~~ | **Сделано.** Вместе с цветом, неоном, номером и дополнениями |
| ~~Маркеры и чекпоинты~~ | **Сделано.** Рисуются обе; чекпоинт вдобавок остался зоной |
| ~~`playAnimation`, `clearTasks`~~ | **Сделано.** Уходят всем, кто игрока видит |
| ~~Насадки на оружие~~ | **Сделано.** Ходят вместе со снаряжением: `addWeaponComponent`, `setWeaponTintIndex` |
| ~~Подгрузка чужих моделей~~ | **Сделано.** Россыпью файлов, как у alt:V: `stream/` и `.meta` рядом |
| ~~События между ресурсами~~ | **Сделано.** `alt.emit` слышат все поднятые ресурсы |
| ~~Метки на карте~~ | **Сделано.** Сервер ставит — игрок видит |
| ~~Предметы (`alt.Object`)~~ | **Сделано.** |
| ~~Одежда, посадка в машину, перестановка и починка машины~~ | **Сделано.** |

**Движение запирает персонажа, и признаками это не лечится.** Проверено живой
игрой, и проверено дважды: `playAnimation` с бесконечной длительностью
отбирает у игрока управление — ни шагу, ни поворота, — а `1 | 16 | 32`
(«повтор, не отбирать управление, дать прервать») его не отпускает. Отпускает
только `clearTasks`, и это работает.

Само по себе это не поломка: так устроена игра, и на этом держатся наручники,
обыск и всё, где персонаж обязан стоять. Но помнить об этом надо тому, кто
пишет режим: движение, которое снимается выходом из зоны, запирает игрока в
ней навсегда — выйти он уже не может. Показательный режим поэтому даёт
движению четыре секунды, а не «до конца».

**Движение спорит с задачами, которыми ведутся куклы, и это остаётся так.**
Идущему игроку движение достанется наполовину: его кукла в это время ведётся
задачей ходьбы, и та движение перебивает. Стоящему — целиком, и это тот случай,
ради которого движения и зовут. Разрешать спор в пользу движения нельзя не
подумав: перестав вести куклу задачей, мы потеряем ходьбу.

Подгрузка моделей была главным из этого списка и закрыта 21 августа 2026:
своё устройство файловой системы, объявление файлов стримингу и описания через
загрузчик данных. Подробности — `docs/altv-parity.md`, раздел 5.

Обвесы машины закрыты там же и тогда же. Внешность назначается целиком одним
описанием — иначе клиент увидел бы машину собранной наполовину, — и с этого
мгновения принадлежит серверу: объявления ведущего о ней отбрасываются.
`getModsCount` при этом отказывает вслух, и это не пробел, а единственный
честный ответ: сколько деталей есть у модели в этом месте, знает игра, а не
сервер.

Привязка сущностей закрыта там же. Состояние, а не распоряжение: сервер помнит
её и пересказывает вошедшим, а клиент накладывает её тогда, когда у него
появятся оба тела, — привязка приходит один раз, а машина может ещё не доехать.
Кольца отвергаются: игра ведёт привязанное пересчётом от родителя, и у двоих,
привязанных друг к другу, пересчёт не кончается никогда.

Своей кости (`ownBone` у alt:V) нет, и это не забывчивость: натива,
привязывающего кость к кости, в сборке 3889 не нашлось. Привязка от этого не
отменяется — она делается по кости цели, — но названная своя кость говорит о
себе один раз в журнал.

Прохожие закрыты там же. Кукла устроена как предмет, а не как чужой игрок: за
предметом не стоит живой человек, и догонять его снимками незачем. Ведущего у
куклы нет, и это решение — она не ходит, не убегает от выстрелов и не садится за
руль. Понадобится ходящая, это будет отдельная работа: ей нужен ведущий, снимки и
задачи, то есть всё устройство машин заново.

Сеть ресурса на клиенте закрыта там же. Девять методов HTTP с обещаниями и
своими заголовками, WebSocket с подпротоколами, автоповтором и всеми четырьмя
событиями. Всё поверх Node, а не своим разбором протоколов.

Проверять её в наборе нельзя — сокет там поднимать запрещено, — поэтому
проверяется то, что решается без сети: имена, заголовки, состояния и отказы.
Живая проверка делалась отдельно и разово: подопытный сервер на localhost, запрос
POST с телом по-русски и круг сообщений по WebSocket. Повторять её стоит тем же
приёмом — временной проверкой, которая в набор не попадает.

**Доводы `syncedMetaChange` приведены к alt:V** — закрыто 21 августа 2026.
Сверено не по памяти, а по `@altv/types-client` в `node_modules` живого режима:
`syncedMetaChange(entity, key, value, oldValue)`. Прежде отсюда уходили род и
номер вместо сущности, и обработчик, написанный под alt:V, получал строку
«player» там, где ждал игрока.

Вместе с этим разошлись три события, которые прежде были одним. Общие метаданные
уходят своим `globalSyncedMetaChange(key, value, oldValue)` — сущности у них нет
вовсе, — а потоковые своим `streamSyncedMetaChange`. Различает их признак в
посылке: хранятся они у нас в одном месте и доходят одинаково, и не назови сервер
род, клиенту пришлось бы либо молчать об одном из двух, либо объявлять оба — и
режим, подписанный на оба, считал бы каждое изменение дважды.

Сущности у клиента может не быть вовсе — предмет и кукла по номеру сессии ему не
известны, — и тогда событие не объявляется: объявить его не с чем. Значение при
этом запоминается как прежде.

## 5. Как проверять

Проверки без игры: `client-js/tests` — машина грузится по имени, как её грузит и
клиент. Игра для них не нужна и не должна быть нужна.

**Сеть машин проверена вживую 22 августа 2026.** Сервер посадил игрока за руль
(`setIntoVehicle`), игрок проехал сто двадцать метров, и второй игрок видел
движение всю дорогу — шестнадцать снимков в секунду, без разрывов. Бот
пересказывает машины поимённо, с положением и числом принятых снимков: по ним и
видно, что машина едет, а не стоит.

**Перестрелка проверена вживую 22 августа 2026.** Игрок в GTA выстрелил в куклу
бота, и цепь прошла целиком: клиент стрелявшего заметил попадание, сервер
применил урон, бот узнал о нём. Здоровье упало с 200 до 50 — ровно на потолок
урона за одно попадание (`maxDamagePerHit`), и это тоже проверка: без потолка
доклад о тысяче единиц убил бы с любым здоровьем.

Бот пересказывает попадания и здоровье сам — по этим двум числам цепь и видно,
не заглядывая в игру.

**Живая проверка без игры — ботом.** `oxymp-botclient` линкует то же клиентское
ядро и входит в сессию по-настоящему: он говорит серверу `__oxymp:ready`, и
потому у него объявляется `playerConnect` и приходит всё, что сервер шлёт
вошедшему. Раз в две секунды он пересказывает, сколько ему прислали меток,
маркеров, точек, движений, внешностей машин, привязок и прохожих, — по этим
числам и видно, дошло ли.

У внешностей машин чисел два, и второе важнее первого: сколько из них несли
тюнинг. Заводскую внешность машина объявляет и сама, а места тюнинга заводскими
не бывают — их назначает только сервер.

Показательный режим стоит так, чтобы бот его задевал: контрольная точка лежит в
двенадцати метрах к востоку от места появления, ровно на круге, по которому бот
ходит. Вход в неё даёт движение и строку в чат.

```powershell
release\server\oxymp-server.exe
build\msvc-x64\bin\Debug\oxymp-botclient.exe --address 127.0.0.1 --port 7788 --seconds 20
```

Живая проверка в игре: поднять `release/server`, запустить
`release/client/oxymp.exe --no-ui --server 127.0.0.1:7790 --nickname oxy`,
смотреть `release/client/logs/client_*.log`. Показательный режим —
`server/resources/altv-demo`: он трогает нативы, страницу, события, метки и
метаданные разом, и по его журналу видно, что сломалось.

**Раскладка не перетирает ресурсы в `release/server/resources`**, если они там
уже есть. Правя `server/resources/altv-demo`, копируйте его туда руками — иначе
проверяется старая копия, и это стоило времени не раз.

**Встроенные файлы слоя alt:V лежат внутри бинарника сервера**, а не рядом с
ним: `alt_server.js`, `alt_objects.js` и соседи вшиваются в него при сборке.
Правка любого из них требует пересборки той конфигурации, которой вы
проверяете, — и `release/server/oxymp-server.exe` берётся из `bin/Release`, а не
из `bin/Debug`. Стоило это получаса поисков: правка была верной, а проверялась
старая копия — и выглядело это как «событие зоны приходит, а сравнение зон не
сходится».

## 6. Что заказано и ещё не сделано

### Свёрток ресурса — сделано целиком

Закрыто 24 августа 2026. Клиентская половина ресурса приезжает к игроку одним
запечатанным свёртком, **кладётся в кеш как есть и не распаковывается никогда**.
Исходники режима перестали лежать у игрока обычным текстом — ровно то, ради чего
свёрток и заводился.

Что где лежит:

- сервер собирает свёрток при запуске и раздаёт его целиком, по одному запросу
  на ресурс (`ResourceStore::addBundle`, было и раньше);
- клиент кладёт свёрток в `cache/bundles/<отпечаток>.resource` и держит
  оглавление в памяти (`client/src/bundle_store.*`);
- файлы читаются по одному, с того места, где свёрток лежит, — целиком в память
  он не берётся.

Читают из него трое, и у каждого своя дверь:

| кто | дверь |
|---|---|
| скриптовая машина | `readResourceFile` через границу ABI и перехваты в `alt_client_bootstrap.js` |
| страницы интерфейса | `cefui::setResourceReader` под схемой `http://resource` |
| устройство файловой системы игры | не нужно: модели и описания в свёрток не идут и остаются файлами |

**Разложенное прежде убирается при первом же входе** (`ResourceCache::shed`).
Иначе обновление ничего бы не изменило: свёрток перестал бы раскладываться, а
восемь тысяч файлов, разложенных старой сборкой, так и остались бы лежать.
Убирается по описи, а не сплошным сносом каталога: рядом лежат модели и
описания, которые ресурсу по-прежнему нужны файлами.

**Адреса файлов внутри свёртка остались обычными файловыми, и это решение.**
Своя схема (`oxymp-resource:`) выглядела бы честнее, но сломала бы всё, что
чужой ресурс делает со своим путём: `__dirname`, `import.meta.url`,
`path.join(__dirname, 'assets')`, счёт `../` от файла к файлу. Адрес остался тем
самым путём, по которому файл лежал бы, разложи мы его; не совпадает только
одно — на диске его нет, и до диска дело не доходит, потому что перехват
отвечает раньше.

**Загрузчик CommonJS до перехватов модулей не достаёт, и это стоило половины
набора.** `Module.registerHooks` объявлен работающим для обоих загрузчиков, и для
модулей ES он действительно работает. Но файл, объявленный оттуда как
`commonjs`, Node идёт читать **с диска сам**, не глядя на выданный ему исходник, —
а на диске файла нет. Снаружи это выглядело так: `import` из свёртка работает,
`require` из того же свёртка не находит ничего.

Отсюда две двери вместо одной:

- `Module._load` — им перехватывается всякий `require`, и модуль собирается
  целиком нашим кодом: разбор имени, компиляция, память собранных;
- `load` в `registerHooks` — им перехватывается `import`, и файл CommonJS
  отдаётся загрузчику модулей ES **переходником**, а не исходником: переходник
  берёт уже собранный объект и выпускает его наружу. Тем же приёмом отдаются и
  модули alt:V (`sourceFor`), так что подмены здесь нет: `import` и `require`
  одного файла дают один и тот же объект, а не два похожих.

Разбор имени свой и повторяет Node в том, что нужно чужим бандлам: окончания
(`./кусок` — это и `кусок.js`, и `кусок.cjs`), каталог с `index` внутри, `main`
из `package.json`, пакеты из `node_modules` внутри свёртка.

Проверок на это восемь (`client-js/tests`, метка `[bundle]`) и шесть на само
хранилище (`client/tests/bundle_store_tests.cpp`). Ни одна не поднимает сокета и
не требует игры: свёрток собирается прямо в проверке, а у ресурса, который в них
поднимается, **на диске нет ни одного файла** — пойди машина хоть раз на диск,
это увидела бы первая же из них.

Круг замкнут разово и вручную, потому что в набор такая проверка не идёт: свёрток
снят с живого сервера по HTTP и открыт клиентским хранилищем. Внутри — те самые
`client/index.js` и `client/ui/index.html`; в самом файле не находится ни строки
исходника и ни одного имени файла.

### Что в свёрток не идёт и почему

Модели, текстуры, звуки и описания остаются отдельными файлами. Причина не в
недоделке: их читает **сама игра**, а свёрток ей не открыть — она умеет `.rpf` и
обычные файлы, и научить её третьему формату нечем. Вдобавок свёрток из чужой
карты вышел бы в гигабайты.

Список нарочно белый (`Server::goesIntoBundle`), наоборот тому, как выбран чёрный
список у клиента: ошибка в белом — файл, оставшийся видимым, ошибка в чёрном была
бы игрой без машины и без карты.

### Архивы `.rpf`: что выяснено и где встало

Сделано и работает:

- сервер раздаёт архивы, в том числе в раскладке RAGE MP — каталог на пак с
  `dlc.rpf` внутри; имя раздачи берётся у каталога;
- клиент проверяет заголовок **до** передачи файла игре;
- игра открывает архив своим `fiPackfile` и вешает его на приставку.

Две настоящие поломки закрыты, и обе стоили вылета:

1. **Битый архив роняет игру внутри неё самой.** Файл с настоящей меткой и
   мусором за ней (ровно 200000 байт — чья-то заглушка) заставил игру прочесть
   из мусора число записей и писать за концом отведённого:
   `0xC0000005` в `GTA5.exe+0x1362ed3`, на `mov [rax+rbx*2], dx`. Отсюда правило:
   всё, что приходит с сервера, проверяется до передачи игре.
2. **Архив с незашифрованным оглавлением (метка `OPEN`) вешает игру намертво.**
   Не отказ, а зависание внутри открытия. Поймано сторожем кадра.

Ещё одно наблюдение, стоившее нескольких вылетов: **архив нельзя
переименовывать**. У части архивов ключ расшифровки оглавления игра выбирает по
имени и размеру файла; под чужим именем она расшифровывает не тем ключом и
получает из оглавления мусор. Поэтому клиент отдаёт игре архив под его настоящим
именем, а отпечаток остаётся только в приставке.

**Где встало.** Игра архив открывает (возвращает согласие), объект заполняет —
в нём и размер файла, и число записей, и имя, — и вешает на приставку: устройство
по ней действительно наше. Но **оглавление в памяти остаётся зашифрованным**:
по указателям на таблицу записей и таблицу имён лежит шифротекст, а не имена.
Поэтому обход не находит ничего и чтение по имени тоже.

Своё устройство при этом ни при чём, и это проверено трассировкой: игра
запросила у него длину, признаки, открыла двумя способами, сделала Seek и
прочла ровно три куска — заголовок (16 байт), таблицу записей и таблицу имён, —
все по верным смещениям и целиком.

Что пробовали и что не помогло: короткая приставка вместо длинной; своя
приставка вместо `dlcpacks:/имя/`; вид архива 0 вместо 3; `MountGlobal` вместо
`fiPackfile::Mount` (и обратно — `Mount` нужен: он не только вешает, но и
запоминает приставку в самом архиве, а признак «пускать к корню» берёт из поля
объекта по смещению `0x114`, которое приходится ставить самим).

Следующий шаг — разобрать `fiPackfile::Init` дизассемблером и найти условие, при
котором она расшифровывает оглавление. Гадать здесь больше нечего: все дешёвые
догадки перебраны.

**CitizenFX этот вопрос не решает, а обходит, и знать это важно до того, как
браться за его исходники.** Его `RagePackfile7` (`code/components/vfs-core`)
читает архив сам и **берёт только незашифрованные**: заголовок с меткой,
отличной от `OPEN` (0x4E45504F) или его собственной `CFXP` (0x50584643), он
отвергает первой же проверкой. То есть у CitizenFX нет ни расшифровки
оглавления, ни ответа на наш вопрос: игру открывать чужой архив он не просит
вовсе — он раскрывает его своей файловой системой и отдаёт игре уже содержимое.

Отсюда развилка, и обе ветви дорогие:

- **Просить игру** (наш нынешний путь). Ключ у неё есть, и только у неё, — но
  условие, при котором она пускает его в ход для чужого архива, до сих пор не
  найдено. Нужен дизассемблер и живая игра под отладчиком.
- **Читать самим** (путь CitizenFX). Устройство у нас для этого уже есть —
  `FileDevice` раздаёт игре россыпь файлов и делает это давно. Но раскрыть мы
  сможем только `OPEN`, а раздают люди не такие.

**Что раздают на самом деле — проверено, а не предположено.** У архива в
`resources/avtr` метка шифрования `0x0FEFFFFF` — это NG: ключ выбирается по имени
файла и его длине из таблиц, вшитых в исполняемый файл игры. Своими силами такой
архив не раскрыть, не унеся эти таблицы с собой; у CodeWalker они лежат отдельным
файлом на несколько мегабайт. Значит вторая ветвь закрывает не то, ради чего всё
затевалось.

Вывод: ветвь «просить игру» остаётся единственной, и дешёвых ходов в ней больше
нет. Работа не в коде, а в отладчике.

**Заодно: `resources/dlcpacks/amgone.rpf` — не архив.** Ровно 200000 байт, а в
начале буквы `RPF7` в том порядке, в котором настоящий архив их не пишет
(у него первым байтом `7`). Это та самая чужая заглушка, о которую игра падала
до проверки заголовка. Проверка её отвергает, и это правильно, но лежит она в
раздаче зря — её стоит убрать, чтобы не искать потом, почему одна машина не
появляется.

### Вложенные архивы

Не сделано: **вложенные архивы**. Архив внутри архива клиент считает и говорит о
нём в журнал, но не открывает: игре для открытия нужен путь на диске, а
вложенный лежит внутри чужого оглавления. Понадобится — придётся либо доставать
его во временный файл, либо открывать устройством поверх устройства.

Не сделано и **содержимое DLC**: `content.xml` внутри архива не разбирается, и
виды описаний оттуда не берутся. Пока вид угадывается по расширению — `.ytyp`
это архетипы, остальное модели, — и для машин с картами этого хватает.
