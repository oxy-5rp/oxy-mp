# Слой, которым сервер программируется извне.
#
# Ни от сети, ни от сервера не зависит намеренно: он знает про сущности и
# события, то есть про смысл, а не про устройство передачи. Благодаря этому его
# можно проверить целиком, не поднимая ни одного сокета, — на подставном ядре.
oxymp_library(script
    SOURCES
        src/entity.cpp
        src/events.cpp
    INCLUDES include
    PUBLIC oxymp::shared
    PRIVATE oxymp::options
)
