#pragma once

namespace oxymp::cefui {

/// Имя сообщения, которым страница говорит с клиентом.
///
/// Лежит отдельно от обоих концов намеренно: посылает его процесс отрисовки
/// страницы, а ждёт браузерный, и это разные исполняемые файлы. Разойдись у них
/// имя — сообщения молча уходили бы в никуда, и искать причину было бы негде.
constexpr const char* kMessageFromPage = "oxymp";

/// Имя сообщения, которым едут события alt.
///
/// Одно на оба направления, и этого достаточно: направление видно по тому, какой
/// процесс сообщение получил. Браузерный получает от страницы — это её alt.emit;
/// процесс отрисовки получает от браузерного — это то, что страница ждёт в
/// alt.on. Два имени вместо одного означали бы, что где-то есть третий случай,
/// а его нет.
///
/// Внутри — имя события и доводы строкой JSON. Строкой, а не списком значений
/// CEF: список умеет хранить только плоские значения, а события несут объекты и
/// массивы — и настройки, и описание сервера. Раскладывать их по списку значило
/// бы завести свой способ передачи объектов поверх уже имеющегося у JSON.
constexpr const char* kAltEvent = "alt";

} // namespace oxymp::cefui
