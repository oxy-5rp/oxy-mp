#pragma once

#include <string>

namespace oxymp::client::game {

/// Страница интерфейса, собранная целиком.
///
/// Разметка и картинка экрана загрузки лежат в разных файлах и должны лежать:
/// одну правят руками, вторая весит полтораста килобайт и открывается
/// просмотрщиком. До CEF они доходят одной строкой — страница попадает туда
/// ссылкой с данными, у которой нет ни файла на диске, ни соседей рядом с ним.
///
/// Отсюда и сборка: разметка встроена в модуль как есть, картинка — как байты, а
/// здесь они соединяются в то, что увидит браузер.
[[nodiscard]] std::string composePage();

} // namespace oxymp::client::game
